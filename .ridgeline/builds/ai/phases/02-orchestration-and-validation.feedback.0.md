# Reviewer Feedback: Phase 02-orchestration-and-validation

## Failed Criteria

### Criterion 7
**Status:** FAIL
**Evidence:** F1, F2, F4, F6, F7, F11, F12, F13, F14, F17, F18, F19, F20 are covered by explicitly-labeled named tests. F3 is covered by C19/C21 (generate.test.ts:377, 395). F5 is covered by C22 (generate.test.ts:360) + F22 (spec_coverage.test.ts:667). F8 is covered by C9/C10 (generate.test.ts:194, spec_coverage.test.ts:155). F9 is covered by C11 (generate.test.ts:164). F15 is covered by the cross-layer integration test. F16 is covered by C32 (spec_coverage.test.ts:466). **F10 (token limit exceeded mid-stream with `finish_reason: 'length'`) has no named test.** Confirmed via grep for `finishReason: 'length'` across the engine test suite — zero matches. The mapping exists at generate.ts:96-97 but no test asserts it.
**Required state:** At least one named test exists (in generate.test.ts, spec_coverage.test.ts, or a sibling test file) that (a) enqueues a mocked provider response with `finishReason: 'length'`, (b) calls `generate` without a schema and asserts `result.finish_reason === 'length'` with the partial content preserved on `result.content`, and (c) if the phase spec's schema-branch interpretation is asserted, also asserts the behavior when schema is set. The test should follow the existing naming convention (e.g., 'F10 token limit exceeded mid-stream returns finish_reason length with partial content').

## Issues

- Failure mode F10 (token limit exceeded mid-stream, `finish_reason: 'length'`) has no named test. Acceptance criterion 7 explicitly requires 'each failure mode F1–F20 from spec §9 has at least one named test asserting the documented behavior'. F10 falls in the 'F6–F15' range that the phase spec declares 'net-new tests landing in this phase'. Grep across packages/engine for `finishReason: 'length'` in test files returns zero matches. The mapping `case 'length': return 'length'` exists at packages/engine/src/generate.ts:96-97 and is behaviorally correct for the no-schema branch, but unverified. F3, F5, F8, F9, F16 are covered by differently-named but behaviorally-equivalent tests (C19/C21, C22, C9/C10, C11, C32); F10 is the one gap. (packages/engine/src/generate.test.ts)
  - **Required:** At least one named test exists (in generate.test.ts, spec_coverage.test.ts, or a sibling test file) that (a) enqueues a mocked provider response with `finishReason: 'length'`, (b) calls `generate` without a schema and asserts `result.finish_reason === 'length'` with the partial content preserved on `result.content`, and (c) if the phase spec's schema-branch interpretation is asserted, also asserts the behavior when schema is set. The test should follow the existing naming convention (e.g., 'F10 token limit exceeded mid-stream returns finish_reason length with partial content').

## What Passed

- Criterion 1: tool_loop.ts implements spec §6.4 pseudocode: sequential execution verified in tool_loop.test.ts lines 430-446 and spec_coverage C7; abort checks at iteration+tool-call boundaries visible in tool_loop.ts:175-178 + 283-286 (throw_if_aborted, throw_if_aborted_in_flight); zod validation before execute in validate_tool_input (tool_loop.ts:294-305) + test at generate.test.ts:194-218 (C10); tool_error_policy branches at tool_loop.ts:389-408 (feed_back) and 402-410 (throw); needs_approval gated at tool_loop.ts:107-162; fail-closed at tool_loop.ts:124-137 with test F20 in generate.test.ts:450; max_steps resolves (not throws) at tool_loop.ts:505-514 with C11 in generate.test.ts:164 asserting 'max_steps_exceeded_before_execution' marker.
- Criterion 2: generate.ts: alias resolution at line 400 before HTTP I/O (F1 test asserts this synchronously); model_resolved stamped in result (generate.ts:702, C1 test); Message[]/system handling in build_initial_messages (generate.ts:69-80, C2 test); effort routed via adapter.translate_effort (generate.ts:432-437) with effort_ignored event (C12 test); per-turn cost via aggregate_cost (generate.ts:737-795, C29 C30 tests); pricing_missing dedup (generate.ts:440, create_pricing_missing_dedup, C32 test); finish_reason precedence per turn (tool_loop.ts:516); usage aggregation via sum_usage (C23 test); schema gating + parse + one-repair (generate.ts:642-662, C3/C4/C5 tests); retry via retry_with_policy (generate.ts:573-597, C19/C21 tests); post-stream-start no-retry (generate.ts:580-591, C22 test); on_chunk failure wraps to on_chunk_error (C15/F11 test); streaming parity tested in test/integration/streaming_parity.test.ts.
- Criterion 3: create_engine.ts: validates providers via build_provider_adapters (line 27-37) with factory throwing engine_config_error (test at create_engine.test.ts:13 for empty api_key); merges DEFAULT_ALIASES + user aliases (line 49-55) and DEFAULT_PRICING + user pricing (line 57-62); exposes register/unregister/resolve/list methods (line 78-117); list_aliases/list_prices return spread copies (line 105, 114) verified by create_engine.test.ts:51 defensive-copy test; provider_not_configured_error thrown at generate time via generate.ts:403-406 + test create_engine.test.ts:77.
- Criterion 4: index.ts exports exactly `create_engine`, `version`, 28 types from types.ts, and 11 error classes from errors.ts (lines 9-54). No provider adapter or internal helper (generate, tool_loop, streaming, trajectory, pricing, aliases, retry, usage, schema) is re-exported. Grepped directly — confirmed.
- Criterion 5: packages/agent-kit/package.json:11 adds '@robmclarty/engine': 'workspace:*'; packages/agent-kit/src/index.ts re-exports create_engine (line 17), 11 errors with engine_aborted_error alias (lines 19-32), 28 types (lines 34-61). tsc passes via pnpm check. Note: the index.test.ts doesn't assert engine surface presence (handoff.md's claim to the contrary), but tsc proves compilation — criterion satisfied.
- Criterion 6: 35 success-criteria tests exist across generate.test.ts, tool_loop.test.ts, create_engine.test.ts, and spec_coverage.test.ts. 34 carry explicit C-labels in test descriptions; C26 ('missing credentials → provider_not_configured_error at call time') is covered by the unlabeled but named test at create_engine.test.ts:77 'throws provider_not_configured_error at generate time for an unconfigured provider'. Tests use fixture pricing (e.g. spec_coverage.test.ts:450 register_price override). Mocking is at the `ai` module boundary via packages/engine/test/fixtures/mock_ai.ts — consistent across the suite. All 339 tests pass.
- Criterion 8: test/integration/streaming_parity.test.ts asserts deeply-equal GenerateResult for the same mocked response with and without on_chunk. Chunk-ordering invariants verified separately by C13 (generate.test.ts:307) and C14 (spec_coverage.test.ts:213).
- Criterion 9: test/integration/with_composition.test.ts covers: (a) chunks flow through ctx.emit (line 79-84, 94); (b) abort propagation via timeout (line 104-141, uses timeout(slow_step, 50) — documented in handoff as a deliberate substitute for SIGINT since run() doesn't expose its controller); (c) LIFO cleanup on engine rejection (line 143-173); (d) trajectory tree includes both composition-layer step span and engine.generate span (line 96-99); (e) cost events observable to userland consumer that accumulates totals (line 175-206). Uses mocks, not gated by RUN_E2E.
- Criterion 10: packages/engine/test/cleanup/sigint.test.ts spawns child-harness.ts via `process.execPath` with a custom ESM resolver (register-ts-resolver.mjs + ts-resolver.mjs) that remaps `ai` and `@ai-sdk/anthropic` to local stubs (ai-stub.mjs, sdk-stub.mjs). Sends SIGINT mid-stream (test line 109), asserts cleanup markers fire (test line 115-116), engine-error.json carries is_engine_aborted_error: true (test line 118-126), and exit-reason.json shows aborted_error (test line 127-131).
- Criterion 11: create_engine.test.ts:51 'list_aliases and list_prices return defensive copies' mutates the returned object and asserts engine state is unaffected (model_not_found_error on 'injected', undefined on 'custom:z').
- Criterion 12: No real-network test is enabled in default CI. pnpm check completed in ~9s with 339 tests — inconsistent with live provider calls. Grepping for RUN_E2E reveals the mocked-boundary pattern throughout.
- Criterion 13: Coverage for packages/engine/src/: statements 90.9% (1004/1104), functions 97.7% (129/132), branches 79.2% (545/688), all ≥70%. Verified via .check/test.json per verifier.
- Criterion 14: Manual-review gate 1 (generate sole entry point): grep for `generateText|streamText|streamObject` returns only generate.ts call sites (lines 274, 328) + comment references in tool_loop.ts and providers/types.ts. Confirmed. Manual-review gate 2 (no mutation of caller inputs): no `opts.messages.push/splice/pop` etc found; messages array is copied via `[...initial_messages]` at generate.ts:656; build_initial_messages spreads each message (generate.ts:76). list_aliases/list_prices return spread copies.
- Criterion 15: `pnpm check` exits 0 (verifier confirmed). All 8 sub-checks green: types PASS 1936ms, lint PASS 888ms, struct PASS 260ms, deps PASS 34ms, dead PASS 1444ms, test PASS 2992ms, docs PASS 507ms, spell PASS 1235ms. 339 tests pass including the streaming parity test, cross-layer integration test, and SIGINT harness.
