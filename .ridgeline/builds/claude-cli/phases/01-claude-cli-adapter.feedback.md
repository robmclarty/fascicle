# Reviewer Feedback: Phase 01-claude-cli-adapter

## Failed Criteria

### Criterion 33
**Status:** FAIL
**Evidence:** `pnpm check` exits 1. types/lint/struct/deps/dead/docs/spell all pass. The `test` phase fails: packages/core/test/cleanup/sigint.test.ts times out at 30s under `vitest run --coverage` for the full suite. Run in isolation the test passes in ~120ms. The pre-phase checkpoint (996b91e) passes the same suite. The added surface (~1200 lines across claude_cli/, even with coverage.exclude) slows vitest transform+import enough that the spawned child harness cannot reach its ready-marker inside the 30s gate.
**Required state:** `pnpm check` exits zero with all 8 checks (types, lint, struct, deps, dead, test, docs, spell) green. The SIGINT harness test must not time out under the coverage-enabled full suite. Acceptable resolutions: reduce the work inside the spawned child's startup path, use a precompiled harness script instead of TS-via-resolver, raise the child-harness ready-timeout bound, or otherwise keep the existing test green without loosening its semantics.

## Issues

- `pnpm check` exits 1 due to a 30s timeout in packages/core/test/cleanup/sigint.test.ts during the coverage-enabled full vitest run. The test passes in isolation (~120ms) and passed on the pre-phase checkpoint under the same full-coverage run; the added claude_cli surface slows vitest startup/transform enough to push the child-harness ready-marker past the 30s gate. (packages/core/test/cleanup/sigint.test.ts)
  - **Required:** `pnpm check` exits zero with all 8 checks (types, lint, struct, deps, dead, test, docs, spell) green. The SIGINT harness test must not time out under the coverage-enabled full suite. Acceptable resolutions: reduce the work inside the spawned child's startup path, use a precompiled harness script instead of TS-via-resolver, raise the child-harness ready-timeout bound, or otherwise keep the existing test green without loosening its semantics.

## What Passed

- Criterion 1: packages/engine/src/providers/claude_cli/types.ts exports AuthMode, ToolBridgeMode, SandboxProviderConfig (bwrap|greywall with network_allowlist + additional_write_paths), ClaudeCliProviderConfig, AgentDef, ClaudeCliCallOptions, ClaudeCliProviderReported with the documented fields and optionality.
- Criterion 2: constants.ts declares CLI_BINARY_DEFAULT='claude', DEFAULT_STARTUP_TIMEOUT_MS=120_000, DEFAULT_STALL_TIMEOUT_MS=300_000, SIGKILL_ESCALATION_MS=2000, CACHE_READ_MULTIPLIER=0.1, CACHE_WRITE_MULTIPLIER=1.25, and CLI_AUTH_ERROR_PATTERNS (Object.freeze-d) with all five required substrings; stderr_is_auth_failure lowercases before comparing.
- Criterion 3: argv.ts:build_cli_argv pushes every option value as a separate array element (-p, --output-format stream-json, --model <id>, --verbose, --setting-sources <csv>, --allowedTools per tool, --resume, --agents <json>, --plugin-dir per dir, --json-schema, --append-system-prompt, extra_args verbatim). No template-literal option interpolation anywhere.
- Criterion 4: auth.ts:build_env never touches process.env, strips ANTHROPIC_API_KEY under 'oauth', sets it from config.api_key under 'api_key', sets it only when present under 'auto'. validate_auth_config throws engine_config_error('api_key is required for auth_mode: api_key', 'claude_cli') synchronously when missing. stderr_is_auth_failure is case-insensitive over CLI_AUTH_ERROR_PATTERNS.
- Criterion 5: sandbox.ts:build_sandbox_plan returns {spawn_cmd,prefix_args}; bwrap path binds /usr,/bin,/lib,/lib64,proc,dev,tmpfs with --unshare-*; greywall path emits --allow-host per network_allowlist and --rw per additional_write_paths. Both terminate the prefix with '--' then the binary.
- Criterion 6: cost.ts:decompose_total_cost weights output + base_input + cached*0.1 + cache_write*1.25; is_estimate:true. allocate_cost_across_turns splits proportional to output_tokens and absorbs the float remainder into the final turn so per_turn_totals sum equals total_cost_usd exactly.
- Criterion 7: stream_parse.ts is a line-buffered parser: consume_line records cli_parse_error on JSON.parse failure and continues; as_event returns undefined for unknown types and handle falls back to record_unknown_event. Step index increments inside handle_assistant when last_event_type==='user_tool_result'. tool_use emits tool_call_start then tool_call_end atomically (no delta).
- Criterion 8: stream_result.ts:build_generate_result maps usage (cache_read_input_tokens → cached_input_tokens, cache_creation_input_tokens → cache_write_tokens; reasoning_tokens absent), populates cost per allocate_cost_across_turns, sets provider_reported.claude_cli = { session_id, duration_ms }, and keeps is_estimate:true.
- Criterion 9: spawn.ts invokes node_spawn with explicit { stdio:['pipe','pipe','pipe'], detached:true, env, shell:false } on every call; per-adapter live_children Set is closure-captured; close handler always removes; SIGTERM→SIGKILL escalator uses SIGKILL_ESCALATION_MS with try/catch around process.kill; startup timer armed at spawn and cleared on first stdout byte; stall timer resets on every stdout chunk; process.on('exit') handler iterates ALL_REGISTRIES and synchronously SIGKILLs live members with try/catch, installed once per process.
- Criterion 10: index.ts:create_claude_cli_adapter returns exactly { kind:'subprocess', name:'claude_cli', supports, generate, dispose } — no build_model/translate_effort/normalize_usage. spawn_runtime is instantiated inside the factory body so two invocations own disjoint Set<ChildProcess>.
- Criterion 11: types.ts adds claude_cli?: ClaudeCliProviderConfig to ProviderConfigMap; index.ts re-exports ClaudeCliProviderConfig, ClaudeCliCallOptions, AgentDef, SandboxProviderConfig, AuthMode, ToolBridgeMode, ClaudeCliProviderReported.
- Criterion 12: aliases.ts DEFAULT_ALIASES now contains cli-opus → claude-opus-4-7, cli-sonnet → claude-sonnet-4-6, cli-haiku → claude-haiku-4-5, and KNOWN_PROVIDERS contains 'claude_cli'.
- Criterion 13: registry.ts BUILTIN_PROVIDERS map now pairs 'claude_cli' with create_claude_cli_adapter; registry.test.ts updated accordingly and the passing test list includes 'claude_cli'.
- Criterion 14: generate.ts (unchanged by this phase) already branches on adapter.kind === 'subprocess' and delegates to adapter.generate(opts, resolved); no provider-specific branches introduced.
- Criterion 15: build_generate_result sets model_resolved to resolved alias (provider + model_id), finish_reason:'stop', cost.total_usd from total_cost_usd, is_estimate:true, usage field mapping per §7.3, and provider_reported.claude_cli = { session_id, duration_ms }. Behavior is in place; end-to-end test is deferred to Phase 2 per spec §11 reference.
- Criterion 16: index.ts:445 emits record_cost(trajectory, step.index, step.cost, 'provider_reported') for every step.cost; no pricing_missing emission anywhere in claude_cli/; allocate_cost_across_turns reabsorbs float remainder into the last turn (cost.ts:88-92) so Σ step cost = total_cost_usd exactly.
- Criterion 17: auth.ts:build_env under 'oauth' deletes ANTHROPIC_API_KEY from the merged env after composing caller_env; config.api_key is not reintroduced in that branch.
- Criterion 18: auth.ts:validate_auth_config throws engine_config_error synchronously for auth_mode === 'api_key' + missing/empty api_key; create_claude_cli_adapter calls it at factory time (index.ts:357), so create_engine surfaces the error synchronously.
- Criterion 19: index.ts:classify_close_error wraps stderr_is_auth_failure stderr in provider_auth_error('claude_cli', ..., { refresh_command:'claude login' }); stderr_is_auth_failure case-insensitively tests each CLI_AUTH_ERROR_PATTERNS entry.
- Criterion 20: index.ts: under default tool_bridge === 'allowlist_only', tool.execute closures are dropped; dropped list is recorded once as { kind:'cli_tool_bridge_allowlist_only', dropped } via trajectory.record. Under 'forbid', any tool with typeof execute === 'function' throws provider_capability_error('claude_cli','tool_execute',...) before spawn.
- Criterion 21: index.ts:count_user_messages + guard throws provider_capability_error('claude_cli','multi_turn_history','use provider_options.claude_cli.session_id instead') when ≥2 user messages, before spawn.
- Criterion 22: index.ts compile_schema forwards z.toJSONSchema(opts.schema) via --json-schema. On first schema parse failure, one repair invocation runs with { ...call_opts, session_id: parsed.session_id } and a repair prompt; second failure throws schema_validation_error. No third attempt.
- Criterion 23: argv.ts pushes --agents with JSON.stringify(call_opts.agents), --plugin-dir per provider_config.plugin_dirs entry, and extra_args verbatim. allowed_tools entries pass through as literals to --allowedTools.
- Criterion 24: index.ts:build_sandbox_plan is called per generate; spawn_cmd becomes 'bwrap' or 'greywall', prefix_args ends with '--' then the binary. classify_close_error maps ENOENT/'not found'/'sandbox' stderr patterns to claude_cli_error('sandbox_unavailable', ...).
- Criterion 25: index.ts:create_option_ignored_dedup is used to emit exactly one option_ignored per call for max_steps, tool_error_policy, on_tool_approval when present; finish_reason is hard-coded to 'stop' in build_generate_result so 'max_steps' cannot occur.
- Criterion 26: spawn.ts: abort listener calls escalate('abort') which SIGTERMs the process group then SIGKILLs after SIGKILL_ESCALATION_MS; wait_close throws aborted_error({ reason: args.abort?.reason }) when terminate_reason === 'abort'. generate in index.ts never returns a partial result on abort — the rejection propagates from run_cli.
- Criterion 27: index.ts: dispose sets disposed=true, aborts every in-flight controller with 'engine_disposed', awaits spawn_runtime.dispose_all (which SIGTERM/SIGKILLs each live child and awaits close). generate synchronously throws engine_disposed_error on the first line when disposed === true. spawn_runtime is per-factory closure so two engines' registries are disjoint.
- Criterion 28: spawn.ts: node_spawn failures are caught and converted to claude_cli_error('binary_not_found', ...). Probing is deferred until first generate — constructor does not spawn (validate_auth_config is the only synchronous work in the factory).
- Criterion 29: rules/no-child-process-outside-claude-cli.yml exists; ast-grep phase passes (struct: 249ms, exit 0).
- Criterion 30: rules/no-provider-sdk-in-claude-cli.yml exists; imports of 'zod' in index.ts are permitted and ast-grep passes.
- Criterion 31: rules/no-process-env-in-core.yml now matches both `process.env` and `process.cwd()` via `any:` list; files glob packages/*/src/**/*.ts covers engine/src. Ast-grep passes.
- Criterion 32: scripts/check-deps.mjs deps phase passes (33ms, exit 0). Engine package.json invariant upheld.
- Criterion 34: `class ` scan of packages/engine/src/providers/claude_cli/** yields 0 code occurrences (only a doc-comment mention). Repo-wide, `class <name> extends Error` remains confined to packages/core/src/errors.ts and packages/engine/src/errors.ts.
