<!--
  Exemplar: the `/** ... */` block above a module-level function.

  Paste one you wrote and would be happy to see copied. Pick a block where the
  first line names what the thing is and the paragraphs under it say why it
  behaves the way it does, so a reader can see how much explanation earns its
  place and how much does not.

  Length: one whole block, header line plus its paragraphs.

  Candidates you already wrote: src/engine/merge_defaults.ts,
  src/mcp/schema_bridge.ts, src/core/runner.ts.
-->

TODO: replace this line with the block.
