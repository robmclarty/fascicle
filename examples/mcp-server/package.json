{
  "name": "@repo/example-mcp-server",
  "version": "0.0.0",
  "description": "Minimal Model Context Protocol server over stdio: registers a few tools and one resource, plus a tiny client that smoke-tests them.",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "tsx src/server.ts",
    "smoke": "tsx src/client.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "*"
  },
  "devDependencies": {
    "tsx": "*"
  }
}
