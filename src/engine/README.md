# engine

AI engine layer for Fascicle. Provider routing, alias resolution, and
`generate()`. See the top-level [docs/](../../docs/) for concepts and
provider-level details (`docs/providers.md`, `docs/concepts.md`).
