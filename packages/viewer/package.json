{
  "name": "@repo/viewer",
  "version": "0.3.4",
  "description": "Fascicle viewer: minimal in-repo dev dashboard for visualizing a fascicle run as it executes. File-tail or HTTP push, single static HTML page, no build step.",
  "type": "module",
  "private": true,
  "bin": {
    "fascicle-viewer": "./src/cli.ts"
  },
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@repo/core": "workspace:*",
    "zod": "^4.0.0"
  }
}
