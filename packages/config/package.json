{
  "name": "@repo/config",
  "version": "0.3.8",
  "description": "Env loader + zod-validated config accessors for fascicle. The only package permitted to read process.env.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run"
  },
  "dependencies": {
    "zod": "^4.0.0"
  }
}
