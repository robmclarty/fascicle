{
  "name": "@repo/agents",
  "version": "0.3.8",
  "description": "Built-in agents for fascicle (reviewer, documenter, researcher) plus the markdown-driven `define_agent` loader. Layered entirely on @repo/core, @repo/composites, and @repo/engine.",
  "type": "module",
  "private": true,
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@repo/composites": "workspace:*",
    "@repo/core": "workspace:*",
    "@repo/engine": "workspace:*"
  },
  "peerDependencies": {
    "zod": "^4.0.0"
  }
}
